import { RoutingDirective } from './routing.directive';

describe('RoutingDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutingDirective();
    expect(directive).toBeTruthy();
  });
});
